# Server
PORT=3000

# PostgreSQL
DB_HOST=localhost
DB_PORT=5432
DB_USER=wilderness
DB_PASSWORD=wilderness
DB_NAME=wilderness_stays

# Redis (optional - falls back to in-memory cache if unreachable)
REDIS_URL=redis://localhost:6379

# Stripe (optional - payments run in mock mode when unset)
# STRIPE_SECRET_KEY=sk_test_xxx

# Auth
JWT_SECRET=change-me-in-production
